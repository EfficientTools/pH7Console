RESUBMISSION FIXES — GUIDELINES 2.1(a) AND 4

Build 5 replaces rejected build 4. We corrected and revalidated the signed application inside the final installer package so the WebView can communicate with its native terminal commands. We also made first-launch recovery explicit: if a PTY cannot start, the center of the window now presents an enabled “Retry Terminal” action and the native error instead of leaving an inert screen.

The command-planning field and deterministic on-device planner now remain available while the bundled language model warms or if model warm-up fails. The model is an enhancement, not a gate for core app controls. At the minimum supported 800 × 600 window size, the sidebar and Local Command Intelligence panel open as overlays instead of compressing the terminal. Settings, History, workspace context, status text, and action rows now wrap or scroll inside the visible window rather than clipping.

NETWORK SERVER ENTITLEMENT — REQUIRED FOR ON-DEVICE LOCAL AI

pH7Console intentionally includes com.apple.security.network.server solely for its bundled on-device language-model runtime. During local-AI initialization, the main app launches the signed, sandbox-inheriting `llama-server` helper. That helper listens only on 127.0.0.1, uses an operating-system-selected ephemeral port, and requires a new random API key for every launch. The main pH7Console process is its only client.

The listener is never bound to 0.0.0.0, ::, a LAN address, or a public interface. It does not accept connections from other devices, advertise a service, or provide web/FTP/file-sharing functionality. No prompt or terminal data leaves the Mac. Because the command-line helper inherits the parent App Sandbox, the parent must include the server entitlement for this loopback listener to function.

To verify the functionality:
1. Launch pH7Console and select any disposable folder with “Open Workspace.”
2. If the right-side panel is hidden, press Command-J to show “Local Command Intelligence.”
3. Enter “show the current git branch” under “Describe What You Want.”
4. Press “Create Safe Command Plan.” The request is processed by the bundled loopback helper and the result is labeled “On-device model” after warm-up.
5. If the left sidebar is hidden, press Command-B. Open Settings → Privacy and review “Authenticated loopback inference,” which shows the current local runtime state and explains the inherited server entitlement.

pH7Console does not require an account, demo user, subscription, external hardware, or network connection.

To review workspace features:
1. Launch the app.
2. Click “Open Workspace” at the lower left.
3. Select any disposable test folder using the macOS folder picker.
4. Enter a shell command in the center input, or enter a supported plain-English task in the local assistant panel.
5. To test optional voice input, press “Voice” beside the planning field, grant Microphone and Speech Recognition access, speak a request, then press “Stop.” The transcript stays editable and no plan is created until “Create Safe Command Plan” is pressed.

The Mac App Store build is intentionally sandboxed. Shell commands operate within macOS permissions and the workspace selected by the reviewer. The app contains no telemetry, advertising, remote AI service, or cloud account.

Command assistance is provided on device by the bundled Qwen2.5-Coder GGUF model and a signed, sandbox-inheriting llama.cpp helper. The helper listens only on an authenticated ephemeral loopback port and is not exposed to the local network. If the model is still warming, deterministic local command patterns remain available and the real terminal stays interactive.

Voice input uses Apple's Speech framework with on-device recognition required in code. It has no cloud fallback, starts only after an explicit button press, stops after at most 30 seconds, stores no audio, and never inserts into or executes in the shell.

Searchable history stores heuristically redacted command text and execution metadata in a SQLCipher-encrypted database inside the app container. Its random 256-bit key is a device-only, non-synchronizing macOS data-protection Keychain item; FTS and WAL pages are encrypted. Command output is not persisted by default. Command-derived workflow adaptation remains in memory and is rebuilt from up to 500 encrypted history records on launch; Clear all removes both. If Keychain or database authentication fails, the app visibly falls back to memory-only history and performs no plaintext database writes.
